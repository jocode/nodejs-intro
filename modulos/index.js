// Con este ejemplo trabajaremos con el módulo del sistema operativo
const os = require('os');

// console.log(os.platform());
// console.log(os.release());
console.log('Memoria libre (Bytes):', os.freemem());
console.log('Memoria total (Bytes):', os.freemem());